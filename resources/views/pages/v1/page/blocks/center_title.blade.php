<div class="row">
    <div class="col-md-8 text-only center-block">
        <h1>
            @if(empty($value))
                {{$title}}
            @else
                {{$value}}
            @endif
        </h1>
    </div>
</div>
